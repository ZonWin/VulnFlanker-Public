{
  "sample_id": "q1-negative-similar-title",
  "expected_vulnerability": {
    "canonical_id": "CVE-2026-Q1-0011",
    "vendor": "Apache",
    "product": "tomcat",
    "kev_status": true,
    "fixed_versions": "10.1.20",
    "required_references_min_count": 0,
    "required_fields": ["canonical_id", "title", "product", "fixed_versions", "remediation"]
  },
  "expected_matches": [
    {
      "agent_id": "q1-agent-negative-nginx-only",
      "status": "not_affected",
      "required_evidence_types": ["product_match"],
      "risk_priority_one_of": ["none"],
      "should_enter_risk_queue": false
    }
  ],
  "quality_expectations": {
    "normalization_must_succeed": true,
    "canonical_id_must_not_drift": true,
    "needs_review_requires_reason": true
  }
}
